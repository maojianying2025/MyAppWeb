{
  "name": "TPA",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "TPA name"
    },
    "regions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Responsible regions (multi-select)"
    },
    "channels": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Responsible channels (multi-select)"
    },
    "seller_rates": {
      "type": "object",
      "description": "Seller rate by region"
    }
  },
  "required": [
    "name"
  ]
}